# 🔐 Required configuration — must be provided for the app to function
GITHUB_TOKEN=github_pat_...             # GitHub personal access token with repo and issue permissions
GITHUB_OWNER=JohnDoe                    # GitHub username or organization name
GITHUB_REPO=example_gh_repo             # Name of the GitHub repository to sync
YOUTRACK_BASE_URL=https://example.youtrack.cloud  # Base URL of your YouTrack instance
YOUTRACK_TOKEN=perm-...                 # Permanent token for authenticating with YouTrack
GITHUB_WEBHOOK_SECRET=secret-webhook   # Secret used to validate incoming GitHub webhooks

# ⚠️ Conditional configuration — at least one of the following must be set
YOUTRACK_PROJECT_SHORTNAME=XYZ         # Short name of the YouTrack project (e.g., "ABC")
YOUTRACK_PROJECT_ID=0-0                # Internal YouTrack project ID (e.g., "0-123")

# 🧪 Optional overrides — only modify if you need custom behavior
GITHUB_API_URL="https://api.github.com"  # GitHub API base URL
WEBHOOK_PORT=3000                         # Port to expose the webhook listener (default: 3000)
